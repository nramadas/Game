// Generated by CoffeeScript 1.6.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["game_object"], function(GameObject) {
    var Player;
    Player = (function(_super) {
      __extends(Player, _super);

      Player.speed = 10;

      Player.height = 40;

      Player.width = 40;

      function Player() {
        Player.__super__.constructor.call(this);
        this.inventory = {};
        return;
      }

      Player.prototype.render_moving_north = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#e51c23";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_moving_south = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#9c27b0";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_moving_east = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#5677fc";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_moving_west = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#ffeb3b";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_stationary_north = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#f9bdbb";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_stationary_south = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#e1bee7";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_stationary_east = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#d0d9ff";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      Player.prototype.render_stationary_west = function() {
        var temp_canvas, temp_context;
        temp_canvas = document.createElement("canvas");
        temp_canvas.height = this.constructor.height;
        temp_canvas.width = this.constructor.width;
        temp_context = temp_canvas.getContext("2d");
        temp_context.fillStyle = "#fff9c4";
        temp_context.beginPath();
        temp_context.arc(this.constructor.height / 2, this.constructor.width / 2, this.constructor.width / 2, 0, 2 * Math.PI, true);
        temp_context.fill();
        return temp_canvas;
      };

      return Player;

    })(GameObject);
    return Player;
  });

}).call(this);
