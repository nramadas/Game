// Generated by CoffeeScript 1.6.3
(function() {
  window.set_interval = function(time, fn) {
    return setInterval(fn, time);
  };

  window.clamp = function(num, lower_bounds, upper_bounds) {
    return Math.min(Math.max(num, lower_bounds), upper_bounds);
  };

  requirejs.config({
    baseUrl: "/static/bin"
  });

  require(["game_controller"], function(GameController) {
    var $canvas, game;
    $canvas = document.getElementById("canvas");
    game = new GameController($canvas);
    game.start();
  });

}).call(this);
